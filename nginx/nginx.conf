worker_processes auto;

events {
	worker_connections 1024;
}

http {
	include /etc/nginx/mime.types;
	default_type application/octet-stream;
	server_tokens off;

	log_format main '$remote_addr - $remote_user [$time_local] "$request" '
					'$status $body_bytes_sent "$http_referer" '
					'"$http_user_agent" "$http_x_forwarded_for"';

	access_log /var/log/nginx/access.log main;
	error_log /var/log/nginx/error.log warn;

	sendfile on;
	keepalive_timeout 65;

	# WebSocket 업그레이드용
	map $http_upgrade $connection_upgrade {
		default upgrade;
		'' close;
	}

	# 앱 서버 (private 서브넷 EC2)
	# Todo: 도메인 구매 시 Cloud Map DNS로 변경, 현재는 단일 서버
	upstream backend {
		# ── 로드밸런싱 전략 ─────────────────────────────────────────
		# 1. Round Robin (기본값): 요청을 순서대로 각 서버에 분산
		server 10.0.11.23:8080;

		# 2. Least Connections: 현재 연결 수가 가장 적은 서버로 요청
		# least_conn;

		# 3. IP Hash: 클라이언트 IP 기반으로 항상 같은 서버로 라우팅 (세션 유지)
		# ip_hash;

		# 4. Weight: 서버에 가중치를 부여해 트래픽 비율 조정
		# server 10.0.11.23:8080 weight=3;
	}

	server {
		listen 80;
		server_name _;

		# 파일 업로드 허용 크기
		client_max_body_size 10m;

		# 헬스체크
		location = /health {
			access_log off;
			return 200 'OK';
		}

		# /api/sse/* → SSE (버퍼링 반드시 off)
		location ^~ /api/sse {
			proxy_pass http://backend;
			proxy_http_version 1.1;
			proxy_buffering off;
			proxy_cache off;
			proxy_set_header Connection '';
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
			proxy_read_timeout 3600s;

		}

		# /api/* → 백엔드 API 프록시
		location ^~ /api/ {
			proxy_pass http://backend;
			proxy_http_version 1.1;
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

		}

		# /ws/* → WebSocket (STOMP) 프록시
		location ^~ /ws/ {
			proxy_pass http://backend;
			proxy_http_version 1.1;
			proxy_set_header Upgrade $http_upgrade;
			proxy_set_header Connection $connection_upgrade;
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
			proxy_read_timeout 3600s;
			proxy_send_timeout 3600s;

		}

		# 그 외 모든 요청 → app으로 프록시 (프론트 정적 파일도 Spring Boot가 서빙)
		location / {
			proxy_pass http://backend;
			proxy_http_version 1.1;
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		}
	}
}
